#reverse Digit - O not working

def decnum(number):
    rev = 0
    while number > 0:
        rem = number % 10
        rev = rev*10 + rem
        number = number // 10
    return rev



print(decnum(1234098))
